{% extends 'base.html.twig' %}

{% block title %}Projet index{% endblock %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th colspan="3" class="headerTable">Projects list</th>
                <th>  
                    <a href="{{ path('projet_new') }}">
                    <button type="button" class="btn btn-danger">
                    <i class="fa fa-plus"></i>
                    </button>  
                    </a>
                </th>
            </tr>
            <tr class="header" >
                <th>Nom</th>
                <th>Nombre d'étudiants</th>
                <th>Note</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projet in projets %}
            <tr>
                <td>{{ projet.nom }}</td>
                <td>{{ projet.nombreEtudiant }}</td>
                 <td>
                {% if projet.note is null %}
                    Il n'y a pas du note
                {% else %}
                    {{ projet.note }}
                {% endif %}
                </td>
                <td>
                    <a href="{{ path('projet_show', {'id': projet.id}) }}">
                        <button type="button" class="btn btn-primary">
                        <i class="fa fa-eye"></i></button> </a>
                    <a href="{{ path('projet_edit', {'id': projet.id}) }}">
                        <button type="button" class="btn btn-secondary">
                        <i class="fa fa-pencil"></i>
                         </button>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
